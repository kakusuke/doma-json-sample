group 'com.example'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'org.seasar.doma', name: 'doma', version:'2.3.1'
    compile group: 'org.postgresql', name: 'postgresql', version:'9.4-1201-jdbc41'
    compile group: 'com.fasterxml.jackson.core', name: 'jackson-core', version: '2.6.0'
    compile group: 'com.fasterxml.jackson.core', name: 'jackson-annotations', version: '2.6.0'
    compile group: 'com.fasterxml.jackson.core', name: 'jackson-databind', version: '2.6.0'

    testCompile group: 'junit', name: 'junit', version: '4.11'
}

/*
ext {
    generated = file("${projectDir}/src/main/generated")
}
sourceSets {
    main {
        java {
            srcDirs generated
        }
    }
}
compileJava {
    doFirst {
        mkdir generated
    }
    options.compilerArgs += ['-s', generated]
}

task cleanGenerated(type: Delete) {
    delete generated
}
*/

processResources.destinationDir = compileJava.destinationDir
//compileJava.dependsOn << [cleanGenerated, processResources]
compileJava.dependsOn << processResources

